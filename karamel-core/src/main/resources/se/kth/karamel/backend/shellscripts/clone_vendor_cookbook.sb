sudo mkdir -p %cookbooks_home%
cd %cookbooks_home% && sudo rm -fr %cookbook_name%
cd %cookbooks_home% && sudo git clone %cookbook_url%
cd %cookbooks_home%/%cookbook_name% && sudo git checkout %branch_name%
cd %cookbooks_home%/%cookbook_name% && sudo berks vendor %vendor_subfolder%